# AGENTS.md

This is a new, empty repository. No commands, config, or architecture to document yet.

When the project gains code, config files, or build tooling, update this file with:
- exact dev/test/lint commands
- package boundaries and entrypoints
- toolchain quirks or repo-specific conventions
- agent configurations: `opencode.json`
